import { StyleSheet } from 'react-native';
import { themeColor, themeFontColor } from '../../assets/styles';

const styles = StyleSheet.create({
  header: {
    backgroundColor: themeColor,
  },
  icon: {
    color: themeFontColor,
  },
  title: {
    color: themeFontColor,
  },
});

export default styles;
